// homePoster.js
// Handles home poster (tiles container) setup and sizing logic for the Math Museums home view.

export function setupHomePoster() {
    // Return the tiles container directly - no intermediate containers needed
    return document.querySelector('.tiles-container');
} 